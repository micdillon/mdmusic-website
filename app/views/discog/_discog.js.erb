$(document).ready(function(){

  $("#discog-area").playlist({
    progressBarWidth: 20,
    playerurl: "swf/drplayer.swf"
  });

  /*$('.playable-link').each(function(idx,el){
    $(el).bind('click', function(ev){
      $('#discog-area').playlist('toggle');
    });
  });*/

}); // end document.ready()
