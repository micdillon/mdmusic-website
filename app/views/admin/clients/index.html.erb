<h1>Listing clients</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clients.each do |client| %>
  <tr>
    <td><%= client.name %></td>
    <td><%= client.description %></td>
    <td><%= link_to 'Show', admin_client_path(client) %></td>
    <td><%= link_to 'Edit', edit_admin_client_path(client) %></td>
    <td><%= link_to 'Destroy', [:admin, client], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Client', new_admin_client_path %>
